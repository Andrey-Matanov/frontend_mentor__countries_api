import React from "react";

const Header = () => {
    return (
        <header style={{ display: "flex" }}>
            <h1>Header</h1>
            <p>Dark mode</p>
        </header>
    );
};

export default Header;
